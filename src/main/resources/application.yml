server:
  port: 8080

spring:
  datasource:
    url: jdbc:h2:file:./data/lista-servicos;AUTO_SERVER=TRUE
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  h2:
    console:
      enabled: true
      path: /h2
